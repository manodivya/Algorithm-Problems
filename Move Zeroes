var moveZeroes = function(nums) {
    
    for(var i = nums.length -1 ; i>=0;i--){
        if(nums[i] === 0){
          nums.push(nums[i]);
            nums.splice(i,1);
            
        }
        
    }
    
};
