var removeElement = function(nums, val) {
    var remArr = nums;
    var len = nums.length;
    for(var i=len-1;i>=0;i--){
        if(remArr[i] === val){
            remArr.splice(i,1);
        }
        
    }  
    return remArr.length;
    
};
